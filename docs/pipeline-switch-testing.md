# 采集管线切换测试记录

日期：2026-08-08

## 自动化验证

- 完整后端测试：10 个测试套件、59 个测试全部通过。
- 覆盖限流状态持久化、方案路由、页码转换、缺少 fakeid、配置默认值、URL 白名单和加密兼容。
- 后端 Nest 构建、前端 TypeScript/Vite 构建、前端 ESLint 均通过。
- 空 SQLite 从 0 执行 10 条迁移通过，包含 `mp_state` 创建和方案字段迁移。

## 绑定验证

- `WereadService` 只查询 `pipeline = 1` 账号。
- `MpService` 的指定账号、轮询账号和失效处理只查询 `pipeline = 2` 账号。
- 账号 API 仅操作当前方案；同 ID 跨方案改绑返回冲突。
- 方案1 token 进入数据库前加密，账号 API 响应不返回 token。
- 同步任务运行中切换方案返回冲突；切换结果写入 `mp_state.active_pipeline`。

## 运行验证

- 生产模式 `/dash` 返回 200。
- 未授权 `platform.pipeline` 返回 401，正确授权返回 200。
- 新建 Docker 数据卷首次启动自动应用全部迁移，`/dash` 与 `platform.pipeline` 均返回 200。
- 空库的方案1和方案2均正确显示“无可用账号”，不会误报方案2已就绪。

## 边界

- 发布回归未重复调用真实微信采集接口，避免消耗配额或触发新的风控。
- 真实上游仍可能调整接口或限制账号；系统保证明确失败、限流保护和人工切换，不承诺绕过平台风控。
